# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'InstClient' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

    pod 'SDWebImage'
    pod 'R.swift'
    pod 'ObjectMapper'
    pod 'Alamofire'

  # Pods for InstClient

  target 'InstClientTests' do
    inherit! :search_paths
   
  end

  target 'InstClientUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end

